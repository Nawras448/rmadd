"""UI sub-controllers extracted from StoreScreen (Milestone 1).

Controllers own widget translation; the domain logic they drive lives in
pure-Python classes beside them (see optimistic_state.OptimisticPackageState).
"""
