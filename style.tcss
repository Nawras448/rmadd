/* ============================================================
   rmadd — Cyberpunk / Neon theme
   Palette: midnight navy bg, purple/magenta borders,
   electric cyan/mint accents, light slate text.
   Core palette is provided by the "cyberpunk" Theme registered
   in features/ui_switch/presentation/tui/app.py; this sheet only
   holds layout + component styling.
   ============================================================ */

/* ---------------- root ---------------- */

Screen {
    layout: horizontal;
    background: $background;
    color: $foreground;
}

Header {
    background: $background;
    outline-bottom: round $primary;
    color: $foreground;
}

HeaderTitle {
    color: $secondary;
    text-style: bold;
}

HeaderClock {
    color: $text-muted;
}

Footer {
    background: $background;
    outline-top: round $primary;
}

FooterKey > .footer-key--key {
    background: $primary;
    color: $background;
    text-style: bold;
    padding: 0 1;
}

FooterKey > .footer-key--description {
    color: $text-muted;
}

FooterKey:hover > .footer-key--key {
    background: $accent;
}

/* ---------------- sidebar / navigation ---------------- */

#sidebar {
    width: 20%;
    min-width: 26;
    max-width: 32;
    height: 100%;
    background: $surface;
    border: round $primary;
    border-title-color: $secondary;
    border-title-align: center;
    padding: 1;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

#sidebar-title {
    border-bottom: heavy $primary;
    text-align: center;
    text-style: bold;
    padding: 1;
    color: $secondary;
}

#sidebar Button {
    width: 100%;
    margin: 0 0 1 0;
    border: none;
    border-bottom: dashed $border-blurred;
    background: transparent;
    color: $foreground;
    text-align: left;
    text-style: none;
}

#sidebar Button:hover {
    background: #a855f7 15%;
    color: #c084fc;
}

#sidebar Button.-primary {
    border-bottom: dashed $accent;
    color: $secondary;
    text-style: bold;
    background: transparent;
}

#sidebar Button.-error {
    color: #f43f5e;
    background: transparent;
}

#content {
    width: 1fr;
    height: 100%;
    padding: 1;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

/* ---------------- generic containers ---------------- */

#store-layout, #detail-layout {
    width: 100%;
    height: 100%;
    padding: 1;
}

#left-panel, #right-panel {
    width: 1fr;
    height: 100%;
}

#full-system-content {
    width: 1fr;
    height: 100%;
    padding: 1;
    overflow-y: auto;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

/* ---------------- tabs ---------------- */

#store-tabs {
    height: 1fr;
    width: 100%;
    padding: 0 1;
}

TabbedContent {
    height: 1fr;
}

TabPane {
    height: 100%;
}

TabbedContent > ContentTabs {
    dock: top;
    height: 3;
    background: $surface;
    border-bottom: round $primary;
}

TabbedContent Tab {
    height: 1;
    padding: 0 2;
    text-align: center;
    color: $text-muted;
    text-style: bold;
}

TabbedContent Tab:hover {
    color: #c084fc;
}

TabbedContent Tab.-active {
    color: $secondary;
    text-style: bold;
}

Underline > .underline--bar {
    color: $accent;
    background: $primary;
}

Underline:focus > .underline--bar {
    background: $primary;
}

TabbedContent > ContentSwitcher {
    height: 1fr;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

TabPane {
    height: 100%;
    border: round $primary;
    border-title-color: #c084fc;
    padding: 1;
    background: $surface;
}

/* ---------------- tables ---------------- */

DataTable {
    height: auto;
    margin: 0;
    background: $surface;
    color: $foreground;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

DataTable > .datatable--header {
    background: #1a1030;
    color: #c084fc;
    text-style: bold;
}

DataTable > .datatable--even-row {
    background: $surface;
}

DataTable > .datatable--odd-row {
    background: #11141f;
}

DataTable > .datatable--cursor {
    background: #134e4a;
    color: #e2e8f0;
}

DataTable:focus > .datatable--cursor {
    background: #0f766e;
    color: #e2e8f0;
}

DataTable > .datatable--hover {
    background: #a855f7 20%;
}

DataTable:ansi > .datatable--header {
    background: #1a1030;
    color: #c084fc;
}

#tools-table {
    height: 1fr;
    margin: 0;
}

#search-table, #installed-table, #tools-table {
    height: 1fr;
    margin: 0 0 0 0;
}

#pane-scroll-tools, #pane-scroll-search, #pane-scroll-installed {
    height: 1fr;
    width: 100%;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

#package-table {
    border: round $primary;
    border-title-color: $secondary;
    padding: 1;
    margin: 1;
    height: 1fr;
    background: $surface;
}

#inner-table, #inner-tools-table {
    height: 100%;
}

/* ---------------- inputs ---------------- */

Input {
    background: $surface;
    border: round $border-blurred;
    color: $foreground;
}

Input:focus {
    border: round #10b981;
    color: $secondary;
}

Input > .input--placeholder {
    color: $text-muted 60%;
}

Input:ansi {
    background: $surface;
    border: round $border-blurred;
    color: $foreground;
}

Input:ansi:focus {
    border: round #10b981;
    color: $secondary;
}

#search-top, #installed-top {
    height: auto;
    align: center middle;
    margin: 0;
    background: $surface;
    border: round $primary;
}

#search-top Input, #installed-top Input {
    width: 1fr;
    height: 3;
    padding: 0 1;
    margin: 0;
}

/* ---------------- filter tabs ---------------- */

#installed-filter-tabs, #search-filter-tabs {
    height: 3;
    background: $surface;
    border-bottom: round $primary;
}

#installed-filter-tabs Tab, #search-filter-tabs Tab {
    height: 2;
    padding: 0 2;
    text-align: center;
    color: $text-muted;
    text-style: bold;
}

#installed-filter-tabs Tab:hover, #search-filter-tabs Tab:hover {
    color: #c084fc;
}

#installed-filter-tabs Tab.-active, #search-filter-tabs Tab.-active {
    color: $secondary;
}

#installed-filter-tabs Underline > .underline--bar,
#search-filter-tabs Underline > .underline--bar {
    background: #2dd4bf;
    color: $background;
}

/* ---------------- buttons ---------------- */

Button, Button.-style-default {
    margin: 0 1;
    text-style: bold;
    background: $surface;
    border: tall $primary;
    color: $text-accent;
}

Button.-style-default:hover {
    background: #a855f7 25%;
    color: #c084fc;
    border-top: tall $primary;
    border-bottom: tall $primary;
}

Button.-style-default:focus {
    border: tall $accent;
}

Button.-style-default.-primary {
    background: #7c3aed;
    border: tall #c084fc;
    color: $background;
}

Button.-style-default.-primary:hover {
    background: #9333ea;
    border-top: tall #c084fc;
    border-bottom: tall #c084fc;
}

Button.-style-default.-error {
    background: #be185d;
    border: tall #f43f5e;
    color: $background;
}

Button.-style-default.-error:hover {
    background: #e11d48;
    border-top: tall #f43f5e;
    border-bottom: tall #f43f5e;
}

Button.-style-default.-success {
    background: #059669;
    border: tall #2dd4bf;
    color: $background;
}

Button.-style-default.-success:hover {
    background: #10b981;
    border-top: tall #2dd4bf;
    border-bottom: tall #2dd4bf;
}

Button.-style-default.-warning {
    background: #b45309;
    border: tall #fbbf24;
    color: $background;
}

Button.-style-default.-warning:hover {
    background: #d97706;
    border-top: tall #fbbf24;
    border-bottom: tall #fbbf24;
}

Button:ansi, Button:ansi.-style-flat, Button:ansi.-style-default,
Button:ansi.-style-flat:hover, Button:ansi.-style-default:hover {
    background: $surface;
    border: tall $primary;
    color: $text-accent;
}

Button:ansi.-style-flat.-primary, Button:ansi.-style-default.-primary {
    background: #7c3aed;
    border: tall #c084fc;
    color: $background;
}

Button:ansi.-style-flat.-error, Button:ansi.-style-default.-error {
    background: #be185d;
    border: tall #f43f5e;
    color: $background;
}

Button:ansi.-style-flat.-success, Button:ansi.-style-default.-success {
    background: #059669;
    border: tall #2dd4bf;
    color: $background;
}

Button:ansi.-style-flat.-warning, Button:ansi.-style-default.-warning {
    background: #b45309;
    border: tall #fbbf24;
    color: $background;
}

#tools-action-bar Button, #search-action-bar Button, #installed-action-bar Button, #actions Button, #action-bar Button {
    margin: 0 1;
}

#tools-action-bar Button, #search-action-bar Button, #installed-action-bar Button {
    min-width: 20;
    padding: 0 2;
    color: #e2e8f0;
    content-align: center middle;
}

/* ---------------- action bars & result strips ---------------- */

#store-topbar {
    dock: top;
    height: 3;
    align: center middle;
    border-bottom: dashed $primary;
    padding: 0 1;
    background: $background;
}

#store-topbar Button {
    margin: 0 1;
}

#tools-action-bar, #search-action-bar, #installed-action-bar {
    dock: bottom;
    height: 5;
    align: center middle;
    border: round $primary;
    padding: 0 1;
    margin: 0;
    background: $surface;
}

#action-bar {
    height: 5;
    align: center middle;
    border: round $primary;
    padding: 0 1;
    margin: 1 0;
    background: $surface;
}

#tools-result, #search-result, #installed-result, #action-result, #action-result-store {
    height: auto;
    min-height: 1;
    background: $surface;
    border-top: dashed $accent;
    text-align: center;
    padding: 0 2;
    width: 100%;
    color: $secondary;
    text-style: bold;
}

.result-scroll {
    height: auto;
    min-height: 1;
    max-height: 30%;
    overflow-y: scroll;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

#action-result-store {
    height: 3;
}

.sel-label {
    padding: 0 2;
    text-style: bold;
    background: $accent;
    color: $background;
    width: auto;
    max-width: 45%;
    text-overflow: ellipsis;
}

#actions {
    align: center middle;
    height: 5;
}

/* ---------------- cards & panels ---------------- */

.monitor-card {
    border: round $primary;
    border-title-color: #c084fc;
    padding: 1;
    margin: 1;
    height: auto;
    background: $surface;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

.detail-card {
    border: round $primary;
    border-title-color: #c084fc;
    padding: 2;
    margin: 1;
    background: $surface;
}

#system-card {
    border: round $primary;
    border-title-color: $secondary;
    padding: 1;
    margin: 1;
    background: $surface;
}

#cpu-graph {
    border: round $primary;
    border-title-color: $secondary;
    padding: 1;
    margin: 1;
    background: $surface;
}

.fs-section {
    border: round $primary;
    border-title-color: #c084fc;
    padding: 1;
    margin: 0 0 1 0;
    height: auto;
    background: $surface;
}

#package-info {
    padding: 1;
}

/* ---------------- toasts ---------------- */

Toast {
    background: $panel;
    border: round $primary;
    padding: 1;
}

Toast:ansi {
    background: $panel;
}

Toast > .toast--title {
    color: $foreground;
    text-style: bold;
}

Toast.-information {
    border-left: outer $accent;
}

Toast.-information .toast--title {
    color: $text-success;
}

Toast.-warning {
    border-left: outer $warning;
}

Toast.-error {
    border-left: outer $error;
}

/* ---------------- install progress panel ---------------- */

#progress-panel {
    width: 1fr;
    height: 1fr;
    border: round $primary;
    background: $surface;
    padding: 1 2;
    margin: 1 2;
}

.progress-title {
    content-align: center middle;
    text-style: bold;
    color: $secondary;
    width: 100%;
    height: 3;
}

#progress-stats {
    height: 5;
    align: center middle;
    border: tall $border-blurred;
    background: $background;
    padding: 0 2;
    margin: 1 0;
}

#progress-bar {
    width: 1fr;
    margin: 0 1;
    color: $secondary;
}

ProgressBar > .bar--bar {
    color: #00ffcc;
    background: $surface;
}

ProgressBar > .bar--complete {
    color: #00ffcc;
    background: $surface;
}

ProgressBar > .bar--indeterminate {
    color: #a855f7;
    background: $surface;
}

.progress-eta {
    content-align: center middle;
    color: #22d3ee;
    text-style: bold;
    width: 16;
    height: 1;
    margin: 0 1;
}

#progress-buttons {
    height: 3;
    align: right middle;
    margin: 0 0 1 0;
}

#btn-progress-cancel {
    width: 14;
    min-width: 14;
}

#progress-console {
    height: 1fr;
    background: $background;
    border: tall $border-blurred;
    padding: 0 1;
    margin: 0;
    color: $foreground;
    scrollbar-color: $primary;
    scrollbar-background: $surface;
}

/* ---------------- appimage install panel ---------------- */

#appimage-panel {
    width: 1fr;
    height: 1fr;
    border: round $primary;
    background: $surface;
    padding: 1 2;
    margin: 1 2;
}

#appimage-title {
    height: 3;
}

#appimage-hint {
    height: 3;
    color: $foreground-muted;
    margin: 0 0 1 0;
}

#appimage-tree {
    height: 1fr;
    border: tall $border-blurred;
    background: $background;
    padding: 0 1;
}

#appimage-status {
    height: 2;
    margin: 1 0 0 0;
}

#appimage-buttons {
    height: 3;
    align: right middle;
    margin: 0;
}

#appimage-buttons Button {
    margin: 0 0 0 1;
}

